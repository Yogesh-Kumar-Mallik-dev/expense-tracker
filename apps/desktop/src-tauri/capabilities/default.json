{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main Expense Tracker window",
  "windows": ["main"],
  "permissions": ["core:default", "powersync:default"]
}
