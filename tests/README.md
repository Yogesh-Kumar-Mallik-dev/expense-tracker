# Central Test Suite

All repository tests live in this workspace, grouped by application or package.
Do not create new `tests/` directories inside apps or packages.

Common commands:

```sh
pnpm test
pnpm test:api
pnpm test:web
pnpm test:desktop
pnpm test:mobile
pnpm test:services
pnpm test:logger
```
