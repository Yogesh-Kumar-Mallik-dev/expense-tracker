# Native UI Usage Guide

Import components through the React Native package condition:

```tsx
import { NativeScreen } from "@expense-tracker/ui-native";
```

Add tests under `tests/ui-native/` and run `pnpm test:mobile`.
